<!DOCTYPE html>
<html lang="en">
{{> head}}
<body>
  {{> nav}}
  <h1>{{title}}</h1>
  <pre>
    // code expamle
    server.get('/', (request, response) => {
      response.sendFile(`${pages}/home.html`)
    })
  </pre>
</body>
</html>